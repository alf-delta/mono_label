# Local font assets

The canonical SVG renderer uses only the bundled local files in this directory:

- `Montserrat-VariableFont_wght.ttf` for label metadata, title, and net weight
- `PPEditorialNew-Italic.otf` for tasting notes

Font roles and asset paths are centralized in `src/label/typography/font-roles.ts`.
